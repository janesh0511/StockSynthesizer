services:
  - type: web
    name: StockSynthesizer
    env: python
    buildCommand: pip install --upgrade pip && pip install --no-cache-dir pandas==2.0.3 numpy==1.24.3 && pip install -r requirements.txt
    startCommand: gunicorn app:app